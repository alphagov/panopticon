<%= f.inputs "Author", :id => "author" do %>
  <%= f.input :author, :label => false, :collection => @people, :as => :select, 
    :input_html => {:class => "span8"}, :include_blank => "Select an author", 
    :hint => 'Who should appear as the author? (not necessarily you!)' %>
<% end %>